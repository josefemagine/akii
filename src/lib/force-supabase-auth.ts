/**
 * Force Supabase Session
 * This is a simple stub to satisfy imports in legacy code.
 * It doesn't actually do anything meaningful.
 */

export function forceSupabaseSession(): void {
  console.log("forceSupabaseSession called - this is a stub function");
}

export default {
  forceSupabaseSession
}; 