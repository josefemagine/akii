import React from "react";

import { dashboardStyles } from "./DashboardPageContainer";
import { cn } from "@/lib/utils";
interface PageHeaderProps {}

export function PageHeader({ title, description, children, className }> {
    return (<"div", { className: cn("flex items-center justify-between", dashboardStyles.sectionSpacing, className>, children: [<"div", { children: [<"h1", { className: "text-3xl font-bold", children: title }>, description && (<"p", { className: "text-muted-foreground mt-1", children: description }>>] }>, children && (<"div", { className: "flex items-center gap-2", children: children }>>] }>>;
}
